# ConsolePlat v2.3.0

## AI 选品

- 新增 `AI 选品` 工作区：通过已登录 Chrome 的 CDP 会话，只读搜索 Temu 候选商品。
- 默认关键词为 `黑白T恤`，可在设置中按行维护多个关键词。
- 每个关键词最多读取三页，页面间等待 2 到 4 秒；按可见销量排序并保留前 30 条。
- 每条入选商品仅下载搜索结果页首图，不打开商品详情页。
- 结果以独立批次保存到用户设置的“程序数据目录”下的 `AI选品` 目录，并导出 CSV、XLSX 与失败清单。
- 遇到登录或验证码时任务暂停，不尝试绕过验证，也不关闭用户的 Chrome。

## 验证边界

- 已完成服务、CLI、设置和 PyQt 页面自动化测试。
- 本机在验证时未监听 `http://127.0.0.1:9222`，因此未进行真实 Temu 页面读取；需要在已登录的 Chrome CDP 会话中进行只读小范围验证。

## 打包状态

本次仅更新源码版本。Windows 安装包仍为 `v2.2.0`，需使用现有打包流程生成新的 `v2.3.0` 包后再发布。
