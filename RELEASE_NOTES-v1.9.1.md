# ConsolePlat v1.9.1

## 增量更新协议基础

- 新增版本化应用运行时与增量更新的完整设计、架构决策记录和统一领域术语。
- 新增严格的应用清单、版本指针、切换事务、活动登记和启动健康记录模型。
- 新增 Ed25519 清单签名与验签，自动更新资产必须先通过签名验证。
- 新增确定性内容分块构建工具，相同输入会生成相同的清单、签名和 ZIP 分块。
- 新增文件大小、SHA-256、ZIP 成员和 Windows 路径安全校验。
- 拒绝目录穿越、绝对路径、备用数据流、保留文件名、符号链接和重解析点。
- 构建私钥仅从环境变量读取，不会写入发布资产。

## 当前更新方式

本版本交付的是第一阶段协议基础，尚未切换现有软件内更新器。`v1.9.1` 仍通过完整 Windows ZIP 安装或覆盖到新目录。

稳定启动器、独立版本目录、共享数据根目录、运行任务阻止安装、启动健康确认和自动回退将在后续阶段交付。

## 使用说明

1. 下载 `ConsolePlat-v1.9.1-windows-x64.zip`。
2. 完整解压到本地目录，不要只提取 `ConsolePlat.exe`。
3. 双击 `ConsolePlat\ConsolePlat.exe`。
4. 保持相邻的 `_internal`、`modules`、`resources` 和 `runtime` 目录完整。

## 验证

- 主项目测试：377 passed。
- 更新协议、签名、分块和版本测试：32 passed。
- Python 编译与更新资产 CLI 检查通过。
