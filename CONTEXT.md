# ConsolePlat Domain Language

ConsolePlat coordinates product preparation, publishing, and post-publishing work across its integrated tools.

## Language

**监控店铺**:
Temu 页面店铺切换菜单中的一个店铺，其名称必须与页面显示名称完全一致；该名称用于选择监控目标和匹配页面切换项，不拥有独立登录凭据。
_Avoid_: 店铺备注名、自定义显示名

**监控账号**:
登录 Temu Agent Seller 使用的一套通用凭据，由所有监控店铺共用；增加、删除或切换监控店铺不改变该凭据。
_Avoid_: 店铺账号、每店账号

**申报价格**:
发布批次中为每个商品统一填写到平台申报价格字段的金额。
_Avoid_: 衣服单价、固定价格

**发布批次**:
用户确认开始上架时确定的一组有效商品及其申报价格；批次开始后，该范围与价格保持不变。
_Avoid_: 当前设置、临时任务

**活跃集成任务**:
由 ConsolePlat 管理且尚未结束的监控采集、生图、AI 改图、发布、上架、申请或合规任务，以及由这些任务启动且仍在运行的子进程。存在活跃集成任务时禁止安装软件更新；普通 Chrome、Excel 等不受 ConsolePlat 管理的程序不属于此范围。
_Avoid_: 其他程序、电脑上正在运行的所有程序

**应用版本目录**:
一份经过完整校验、可由稳定启动器独立加载的 ConsolePlat 应用内容。软件更新先生成新的应用版本目录，再切换当前版本指针；不会原地覆盖正在运行的应用内容。系统至少保留当前版本和上一版本，以便启动失败时回退。
_Avoid_: 覆盖安装、直接替换程序库

**共享数据根目录**:
位于 ConsolePlat 所在磁盘、独立于应用版本目录的一套持久数据目录，由所有应用版本共同读取。它保存用户配置和业务数据；版本更新只迁移配置结构、补充缺省字段并保留用户已有值，不用新版默认配置覆盖用户配置。
_Avoid_: 版本配置、随版本复制配置、重置配置

**活动登记表**:
ConsolePlat 对尚未结束的集成任务和受管子进程的统一登记。每条活动记录说明任务身份、用途、开始时间、进程信息和是否阻止更新；更新流程以此作为唯一的运行状态判断依据。
_Avoid_: 页面运行字段、其他程序列表

**更新锁**:
安装更新进入组装、切换或重启阶段前取得的全局互斥状态。更新锁存在时不能启动新的集成任务；存在阻止更新的活动时不能取得更新锁。
_Avoid_: 强制停止任务、只禁用更新按钮

**稳定启动器**:
安装目录中的固定入口，负责单实例协调、选择应用版本、启动健康观察和失败回退。它不承载日常 UI 或业务功能，只有启动协议或安全机制变化时才通过独立通道低频更新。
_Avoid_: 主程序、业务插件

**应用运行时**:
某个 ConsolePlat 版本的完整 UI、业务代码、集成 adapter 和所需依赖。它作为应用版本目录中的整体被加载和更新，不等同于可单独启停的业务插件。
_Avoid_: 插件包、程序库补丁
