# ConsolePlat v2.2.0

## 自动上架五档自定义克重

- 自动上架的“运行设置”新增第 1 至第 5 档独立克重输入框，首次默认值为 `142 / 147 / 152 / 157 / 162g`。
- 五档克重会保存到本机运行设置，重新打开软件后自动恢复。
- 开始批量上架前，确认框和运行状态会显示本批次冻结的克重组合。
- 自动上架流程按第 1 至第 5 个重量输入框的顺序，逐项填写自定义克重；不再固定按 `142g` 起、每档加 `5g` 推算。
- PutawayAiRobot 内嵌模块版本升级至 `3.8`。

## 验证

- 自动上架克重相关 pytest：22 passed。
- PutawayAiRobot 启动依赖检查与 Python 编译检查通过。
- 离屏 PyQt 冒烟检查通过，未连接浏览器或执行真实发布。

## 使用说明

1. 下载 `ConsolePlat-v2.2.0-windows-x64.zip`。
2. 完整解压 ZIP，不要只提取 `ConsolePlat.exe`。
3. 运行 `ConsolePlat\ConsolePlat.exe`。
4. 在“自动上架 -> 运行设置”中填写第 1 至第 5 档克重并保存。
5. 第一次使用新克重时，建议仅发布 1 条商品确认店小秘页面的规格顺序。
